if input.keySpacePress {
	instance_destroy()
	network.generate_alien(x, y)
}

//if input.keySpacePress {
//	if app.cameraObject == player app.cameraFocusObject(demon, "~", 0.2)
//	else app.cameraFocusObject(player, "~", 0.2)
//}